# 待办清单（重构与稳定化）

目标：把项目从“能跑”提升到“稳定、可维护、可扩展”。

## P0 - 立即执行（崩溃与关键逻辑）

- [ ] 为核心流程补回归测试：加载谱面 -> 开始游戏 -> 判定 -> 结算。
- [x] 修复 `core/game_engine.py` 中 `_check_note_hit` 重复定义导致的覆盖问题。
- [ ] 验证长按音符（Hold）在修复后行为是否正确（提前松键、结束判定、漏判）。
- [ ] 统一源码注释与日志编码为 UTF-8，清理乱码文本。
- [x] 为 `GameEngine.end_game` 添加切屏失败保护（异常时记录日志并不中断主循环）。

## P1 - 错误处理强化

- [ ] 在核心路径中，把“吞异常”替换为结构化日志 + traceback。
- [ ] 对可恢复错误增加用户可见提示（音频缺失、谱面缺失、资源加载失败）。
- [ ] 统一错误分类：配置、资源、音频、谱面、UI、Mod。
- [ ] 新增错误处理规范文档：`docs/error-handling.md`（重试、降级、终止策略）。

## P1 - 性能优化

- [x] 优化 `GameEngine._update_notes`：从“每帧全量遍历”改为“游标/窗口扫描”。
- [ ] 按轨道与时间预索引音符，降低命中检测开销。
- [ ] 对热点路径做基线性能测量（更新循环、谱面解析、音频加载）。
- [ ] 在 `scripts/` 增加轻量性能基准脚本。

## P2 - 可维护性重构

- [ ] 拆分 `core/game_engine.py`：状态机、输入处理、判定流程、回调管理分模块。
- [ ] 清理无用导入、重复逻辑，补全关键类型标注。
- [ ] 引入静态检查（`ruff`，可选 `mypy`）。
- [ ] 增加 `CONTRIBUTING.md`（分支规范、提交规范、测试要求）。

## P2 - Bug 债务清理

- [ ] 建立可复现 Bug 列表（严重级别、复现步骤、责任模块）。
- [ ] 优先修复高频玩法问题（判定、暂停恢复同步、音画同步）。
- [ ] 修复 Mod 安全问题（权限失败处理、沙箱边界检查）。
- [ ] 每修一个 P0/P1 Bug，补一条回归测试。

## P3 - 功能演进

- [ ] 补全谱面格式文档与校验工具。
- [ ] 强化 Mod 隔离与权限策略。
- [ ] 增加资源热重载与缓存失效策略。

## 里程碑

1. M1：完成全部 P0，并覆盖核心流程回归测试。
2. M2：完成 P1，达到“错误可定位、性能可量化”。
3. M3：推进 P2/P3，准备版本化发布。
